import { MineBoardCellPipe } from './mine-board-cell.pipe';

describe('MineBoardCellPipe', () => {
  it('create an instance', () => {
    const pipe = new MineBoardCellPipe();
    expect(pipe).toBeTruthy();
  });
});
